<Window x:Class="QstReport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QstReport"
        mc:Ignorable="d"
        Title="Bilan hebdomadaire QST V3"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Content="Générer le rapport" Command="{Binding CreateReportCommand}" Margin="5"/>

        <ProgressBar Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Value="{Binding CurrentProgress}" Width="340" Height="30" Margin="0,0,0,5" VerticalAlignment="Center"/>

        <TextBlock Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Text="{Binding CurrentProgressText}" FontWeight="Bold" Height="25" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0"/>
    </Grid>
</Window>
